============
Installation
============

At the command line::

    $ easy_install django-knocker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-knocker
    $ pip install django-knocker
